# Go-Live fryhigh.de · 11.09.2026 · Schrittliste für Joschka

Quelle: `50_threads/2026-09-11_fry-high-website-launch/analyse/05-deploy-stand.md` §5–§6, verdichtet und um die Worker-Änderungen vom 11.09. ergänzt. Reihenfolge einhalten. Alles, was Claude ausführt, läuft erst nach deinem `wrangler login`. Zeiten sind Schätzungen.

## 0. Was heute anders ist als im Mai

- Alle drei Worker (Catering, Tisch, Spiel) haben neue Variablen und müssen neu deployt werden.
- Catering- und Tisch-Worker melden ohne `RESEND_API_KEY` jetzt **503 `mail not configured`** statt Scheinerfolg. Ohne Resend-Key zeigt die Site also den Mail-Link auf info@fryhigh.de, keine Fake-Bestätigung.
- Der Game-Worker braucht **vor** dem Deploy eine D1-Migration (neue Spalten), sonst antworten Rangliste und Gutschein-Check mit 500.
- Der Magic-Link zeigt jetzt auf den Worker selbst (`VERIFY_BASE`), nicht mehr auf `fryhigh.de/verify`.
- **Neues Cloudflare-Konto (Entscheidung 11.09., 17:24):** Das Konto vom Juni ist nicht mehr zugänglich. Alles wurde im Konto joschkatraue@gmail.com (Account `f106522fba11390a857cf69cd9df6624`) neu angelegt: Pages-Projekt `fryhigh` (`fryhigh.pages.dev`), D1 `fryhigh-game` (neu, leer, volles Schema), workers.dev-Subdomain `fryhigh-bhv`. Die alten Adressen `fryhigh-site.pages.dev` und `*.fryhigh.workers.dev` laufen im fremden Konto weiter, werden aber nicht mehr verlinkt.

## 1. Wrangler-Login (Joschka, 2 Min) — erledigt 11.09. 17:22

```
cd "C:\AI WORKSTATION\10_businesses\fry-high\code\fryhigh-site"
npx wrangler login
npx wrangler whoami
```

`whoami` muss `joschkatraue@gmail.com` und Account `f106522f…` zeigen. Achtung: Im Browser war zuerst ein anderes Google-Konto aktiv; bei falschem Konto `npx wrangler logout` und neu einloggen.

## 2. Cloudflare-Zone anlegen, Mail-Zettel abgleichen — erledigt 11.09. 21:28–21:44 (Claude)

Zone `fryhigh.de` (ID `10d976e808c1006a4330f37e6d5c4743`) liegt im Konto joschkatraue mit 9 Einträgen: CNAME `@`/`www` → `fryhigh.pages.dev` (proxied), MX udag 10/20, SPF udag, DMARC `p=none`, Resend-DKIM `resend._domainkey`, Resend `send` MX/SPF. Nameserver der Zone: **`shane.ns.cloudflare.com`** und **`sierra.ns.cloudflare.com`**. Der ursprüngliche Text bleibt als Referenz:

### 2 (alt). Cloudflare-Zone anlegen, Mail-Zettel abgleichen (Joschka, 15 Min)

1. dash.cloudflare.com → „Add a domain“ → `fryhigh.de` → Free-Plan.
2. Cloudflare scannt die udag-Zone. **Vor „Continue“** diese Records prüfen, alle auf „DNS only“ (graue Wolke):

```
fryhigh.de.   MX  10 mx00.udag.de.
fryhigh.de.   MX  20 mx01.udag.de.
fryhigh.de.   TXT "v=spf1 include:_smtp.udag.de ~all"
```

Optional, falls genutzt: `mail.fryhigh.de` und `webmail.fryhigh.de` A `89.31.143.90` (DNS only). Die Park-A-Records `@` und `www` auf `89.31.143.90` löschen; Pages ersetzt sie später durch CNAMEs. Empfehlung ohne Pflicht: `_dmarc` TXT `v=DMARC1; p=none; rua=mailto:info@fryhigh.de`.

3. Die zwei Cloudflare-Nameserver notieren.

Fehlen die MX nach dem Wechsel, kommt ab Propagation keine Mail mehr an, auch keine Catering-Anfrage. Das ist das eine Risiko, das nicht rückholbar ist.

## 3. Nameserver bei united-domains umstellen (Joschka, 10 Min; Wirkung 1–48 h) — OFFEN, nur Joschka (Login)

united-domains.de → Einloggen (oben rechts) → `fryhigh.de` → Nameserver → „eigene Nameserver“ → eintragen: `shane.ns.cloudflare.com` und `sierra.ns.cloudflare.com`, die drei udag-Nameserver raus, speichern. Cloudflare meldet die Zone per Mail als „Active“. Mail läuft währenddessen weiter, weil die MX-Werte gleich bleiben.

Rückfall, falls der Wechsel heute nicht geht: nur `www.fryhigh.de` als CNAME auf `fryhigh.pages.dev` bei udag eintragen und in Pages als Custom Domain anhängen; Apex bleibt dann Parkseite. Dann müssten `ALLOW_ORIGIN` und `site` in `astro.config.mjs` auf `https://www.fryhigh.de` umgestellt werden. Nur Notlösung.

## 4. Pages Direct Upload (Claude, 5 Min, nach deinem Go)

Nach grünem Build der Integrator-Phase:

```
npx wrangler pages deploy dist --project-name fryhigh --branch main
```

Danach zeigt `fryhigh.pages.dev` den neuen Stand. Rollback über Pages → Deployments. Git-Anbindung des Pages-Projekts ist der nachhaltige Weg, aber nicht heute.

## 5. Custom Domain und Redirect www — erledigt 11.09. 21:35 (Claude): `fryhigh.de` und `www.fryhigh.de` hängen am Pages-Projekt `fryhigh` (Status pending bis Nameserver-Wechsel), Redirect Rule `www` → `https://fryhigh.de/<pfad>` 301 ist aktiv. Alter Text zur Referenz:

### 5 (alt). Custom Domain und Redirect www (Joschka, 5 Min + Zertifikat)

Pages-Projekt → Custom Domains → `fryhigh.de`, dann `www.fryhigh.de`. Cloudflare legt die CNAMEs an, Zertifikat in etwa einer Minute. Dann Rules → Redirect Rules: `www.fryhigh.de/*` → `https://fryhigh.de/$1` (301). Die Worker akzeptieren beide Origins, der Redirect ist trotzdem sauber für SEO.

## 6. Resend (Joschka nur noch der API-Key)

1. Domain `fryhigh.de` existiert im Resend-Konto joschkatraue seit 28.06. (Region eu-west-1). Erledigt 11.09. 21:45: DKIM, `send`-MX und `send`-SPF stehen in Cloudflare-DNS, „Verify DNS Records“ ist angestoßen (Status Pending; Resend prüft stundenlang weiter und wird grün, sobald die Nameserver auf Cloudflare zeigen).
2. **Offen, nur Joschka:** resend.com → API Keys → Create API Key (Name `fryhigh-worker`, Permission „Sending access“, Domain `fryhigh.de`) → Key kopieren, **nicht in den Chat**, direkt in Schritt 7 eintippen.
4. Absender müssen unter der verifizierten Domain liegen: `catering-form@fryhigh.de`, `tisch-form@fryhigh.de`, `spiel@fryhigh.de` (stehen in den drei Wrangler-Configs).

## 7. Secrets setzen (Joschka, 5 Min, eigenes Terminal)

In `infrastructure/cloudflare`:

```
npx wrangler secret list
npx wrangler secret put RESEND_API_KEY
npx wrangler secret put RESEND_API_KEY --config wrangler-table.toml
npx wrangler secret put RESEND_API_KEY --config wrangler-game.toml
npx wrangler secret put CREW_PIN        --config wrangler-game.toml
```

`CREW_PIN`: 4 bis 6 Ziffern, die Crew teilt sich einen. Optional, nur wenn MailerLite eingebunden wird: `MAILERLITE_API_KEY --config wrangler-game.toml` plus `MAILERLITE_GROUP_ID` in `wrangler-game.toml`.

Die neuen Variablen stehen als `[vars]` in den Configs und kommen mit dem Deploy mit, kein Secret nötig: `ALLOW_ORIGIN` (Liste), `VERIFY_BASE`, `SITE_BASE`, `VOUCHER_KIND`, `VOUCHER_LABEL`, `VOUCHER_VALUE_EUR` (15, Warenwert-Deckel), `REDEEM_LOCATION`, `SCORE_HARD_CAP` (20000), `SCORE_PER_SECOND_MAX` (40), `TERMS_VERSION`.

## 8. D1-Schema — erledigt 11.09. 17:32 (neue leere DB, volles Schema)

Weil die D1 im neuen Konto leer war, lief `npx wrangler d1 execute fryhigh-game --remote --file=./game-schema.sql` komplett durch (Beweis: `num_tables: 4`). Die Migration unten ist nur noch relevant, falls jemals wieder eine DB vom Mai-Stand gehoben werden muss:

```
npx wrangler d1 execute fryhigh-game --remote --command "ALTER TABLE scores ADD COLUMN terms_version TEXT; ALTER TABLE scores ADD COLUMN terms_accepted_at INTEGER; ALTER TABLE scores ADD COLUMN age_confirmed INTEGER NOT NULL DEFAULT 0; ALTER TABLE scores ADD COLUMN newsletter_confirmed_at INTEGER; ALTER TABLE scores ADD COLUMN hidden INTEGER NOT NULL DEFAULT 0; ALTER TABLE vouchers ADD COLUMN kind TEXT; ALTER TABLE vouchers ADD COLUMN label TEXT; ALTER TABLE vouchers ADD COLUMN value_eur INTEGER; ALTER TABLE vouchers ADD COLUMN redeem_location TEXT;"
```

Optional danach: `--command "ALTER TABLE sessions DROP COLUMN ip_hash;"`. Prüfen: `--command "PRAGMA table_info(scores);"` muss `hidden` und `terms_version` zeigen.

## 9. Worker deployen — erledigt 11.09. 17:35

```
cd infrastructure/cloudflare
npx wrangler deploy
npx wrangler deploy --config wrangler-table.toml
npx wrangler deploy --config wrangler-game.toml
```

Live: `catering-form` (Version ad9d363a), `table-reservation` (7f7654b9), `fryhigh-game` (e7bbe25b, Cron `0 23 * * SUN`, D1-Binding `env.DB`) unter `https://<name>.fryhigh-bhv.workers.dev`. Die Subdomain `fryhigh-bhv` wurde per API angelegt, weil das Konto noch keine hatte. Nach jeder Config-Änderung (z. B. Secrets bleiben, Vars nicht) denselben Befehl wiederholen.

Achtung Cron: Ein manueller Trigger des Game-Workers unter der Woche friert die laufende Woche ein (Verify wird danach abgelehnt). End-to-End-Test der Gewinnerkette daher nur mit einer Testwoche oder Montag früh zwischen 01:00 und 05:00 Uhr.

## 10. Smoke-Test (Claude per curl, Joschka für Mail-Empfang, 15 Min)

- `https://fryhigh.de` 200, `http→https` 301, `www→apex` 301.
- `/impressum/`, `/datenschutz/`, `/spiel/teilnahmebedingungen/` erreichbar; unbekannte Route → eigene 404.
- `/robots.txt` enthält `Disallow: /crew/`; `/sitemap-index.xml` da.
- `nslookup -type=MX fryhigh.de` liefert weiter `mx00/mx01.udag.de`.
- Worker: `curl -s -X POST https://fryhigh-game.fryhigh-bhv.workers.dev/session/start -H "Origin: https://fryhigh.de"` → 200 mit `Access-Control-Allow-Origin: https://fryhigh.de`; dasselbe mit `Origin: https://fryhigh.pages.dev` → Echo dieser Origin; mit fremder Origin → `https://fryhigh.de`. (Stand 17:36: `/leaderboard` 200 `top: []`, `/rank?score=5` 200, `/session/start` 200, Catering-POST ohne Felder 422.)
- `GET /rank?score=0` → `{ rank, total, week_key }`. `GET /leaderboard` → `top: []` oder Einträge ohne E-Mail-Fragmente.
- Catering-Formular ohne Key → 503 und Hinweis mit Mail-Link auf der Site; mit Key → Mail bei info@fryhigh.de (nur nach deinem Go, sendet echt).
- Spiel-Kette nur nach Go und mit deiner Test-Adresse: spielen → eintragen → Bestätigungsmail → Link → `/verify` zeigt „Bestätigt“ → Rücksprung `/spiel?verified=1` → Score in der Rangliste → Crew-Tool `/crew/vouchers` mit PIN prüft einen Code.
- Rich-Results-Test und OG-Vorschau wie in `docs/deploy-cloudflare.md` §8.

## 11. Was offen bleibt

- MailerLite: Account, Gruppe, Double-Opt-in-Einstellung für API-Kontakte, Formular-IDs für die Startseite (nicht launch-kritisch).
- Anwaltliche Abnahme von Teilnahmebedingungen und Datenschutz.
- Ob die Wochenwertung scharf geht (`contest_live` in `src/content/spiel.md`), entscheidet erst der End-to-End-Test aus Punkt 10.
